/// <reference types="react" />
import { CategoryComponentProps } from '../../models/models';
/**
 * Cotenendor de los elementos. define si se va a emplear un contenedor
 * con background image, color o image item
 * @param props
 */
export default function Container(props: CategoryComponentProps): JSX.Element;
