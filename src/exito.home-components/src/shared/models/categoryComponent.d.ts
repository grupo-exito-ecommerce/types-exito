export interface CategoryImage {
    image: string;
    position: string;
    show_category_image: boolean;
}
export interface CategoryMainBackground {
    categoryBackgroundColor?: string;
    categoryBackgroundImage: string;
    showColorOrImage: boolean;
}
