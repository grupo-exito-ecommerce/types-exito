/**
 * Variables para realizar la consulta de la
 * información adicional de los aliados de
 * MasterData.
 */
export declare const ADDITIONAL_ALLIES_INFO_VARS: {
    acronym: string;
    fields: string[];
    pageSize: number;
};
export declare const ADDITIONAL_FLAGS_INFO_VARS: {
    acronym: string;
    fields: string[];
    pageSize: number;
};
