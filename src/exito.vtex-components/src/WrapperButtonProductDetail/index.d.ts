import { FC, ReactElement } from 'react';
interface WrapperProps {
    children: ReactElement[];
}
declare const WrapperButtonProductDetail: FC<WrapperProps>;
export default WrapperButtonProductDetail;
