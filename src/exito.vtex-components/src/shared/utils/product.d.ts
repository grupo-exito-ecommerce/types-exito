/**
 * Calculate the discount in base of the product prices
 * @param firstPrice - SellingPrice
 * @param secondPrice - ListPrice
 */
export declare const calculateDiscount: (firstPrice: number, secondPrice: number) => number;
