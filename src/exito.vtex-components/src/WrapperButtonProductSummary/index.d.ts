import { FC, ReactElement } from 'react';
interface WrapperProps {
    children: ReactElement[];
}
declare const WrapperButtonProductSummary: FC<WrapperProps>;
export default WrapperButtonProductSummary;
