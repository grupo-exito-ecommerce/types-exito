interface WrapperModalProps {
    state: boolean;
    handleClose: () => void;
    salesChannel: string;
}
declare const WrapperModal: (props: WrapperModalProps) => JSX.Element;
export default WrapperModal;
